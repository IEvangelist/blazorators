namespace Blazor.DOM;

// Explicit profile-boundary stubs for ambiguous interface-only aliases that
// remain deferred until authoritative inbound DOM branding is available.
public readonly struct ReadableStreamController<T>;

public readonly struct ReadableStreamReader<T>;

public interface IReadableStreamAsyncIterator<T>;
