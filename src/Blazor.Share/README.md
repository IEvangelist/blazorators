# Blazor.Share

Generated Web Share API bindings for Blazor Server and hosting-neutral async JavaScript interop.

Register the capability with `services.AddShareCapability()` and inject `IShareCapability`.
