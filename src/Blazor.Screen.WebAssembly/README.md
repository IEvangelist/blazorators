# Blazor.Screen.WebAssembly

Generated Screen and Screen Orientation API bindings with async and in-process sync roots for Blazor WebAssembly.

Register the capability with `services.AddScreenCapability()` and inject `IScreenCapability`.
