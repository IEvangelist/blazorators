# Blazor.Share.WebAssembly

Generated Web Share API bindings with async and in-process sync roots for Blazor WebAssembly.

Register the capability with `services.AddShareCapability()` and inject `IShareCapability`.
