# Blazor.Clipboard.WebAssembly

Generated Clipboard API bindings with async and in-process sync roots for Blazor WebAssembly.

Register the capability with `services.AddClipboardCapability()` and inject `IClipboardCapability`.
