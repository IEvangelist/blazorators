# Blazor.WakeLock

Generated Screen Wake Lock API bindings for Blazor Server and hosting-neutral async JavaScript interop.

Register the capability with `services.AddWakeLockCapability()` and inject `IWakeLockCapability`.
