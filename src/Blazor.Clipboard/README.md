# Blazor.Clipboard

Generated Clipboard API bindings for Blazor Server and hosting-neutral async JavaScript interop.

Register the capability with `services.AddClipboardCapability()` and inject `IClipboardCapability`.
