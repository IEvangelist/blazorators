# Blazor.WakeLock.WebAssembly

Generated Screen Wake Lock API bindings for Blazor WebAssembly with in-process synchronous dispatch for non-Promise operations.

Register the capability with `services.AddWakeLockCapability()` and inject `IWakeLockCapability`.
