# Blazor.Screen

Generated Screen and Screen Orientation API bindings for Blazor Server and hosting-neutral async JavaScript interop.

Register the capability with `services.AddScreenCapability()` and inject `IScreenCapability`.
