<Project Sdk="Microsoft.NET.Sdk">
  <!-- Compile-verification project for the generated Clipboard profile contracts. -->
  <!-- Generated by Blazor.DOM.CSharpGenerator — do not add hand-written code here. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AllowUnsafeBlocks>false</AllowUnsafeBlocks>
    <AssemblyName>Blazor.DOM.Clipboard.Contracts</AssemblyName>
    <RootNamespace>Blazor.DOM</RootNamespace>
    <Description>Generated Clipboard API contracts for Blazor DOM bindings.</Description>
    <IsPackable>false</IsPackable>
    <!-- Disable SDK default glob so we control exactly what compiles. -->
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
  </PropertyGroup>
  <!-- Include all generated .g.cs files from the Clipboard profile output -->
  <ItemGroup>
    <Compile Include="**/*.g.cs" />
  </ItemGroup>
</Project>
