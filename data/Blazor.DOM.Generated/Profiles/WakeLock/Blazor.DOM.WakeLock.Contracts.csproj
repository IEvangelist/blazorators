<Project Sdk="Microsoft.NET.Sdk">
  <!-- Compile-verification project for the generated WakeLock profile contracts. -->
  <!-- Generated by Blazor.DOM.CSharpGenerator — do not add hand-written code here. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AllowUnsafeBlocks>false</AllowUnsafeBlocks>
    <AssemblyName>Blazor.DOM.WakeLock.Contracts</AssemblyName>
    <RootNamespace>Blazor.DOM</RootNamespace>
    <Description>Generated WakeLock API contracts for Blazor DOM bindings.</Description>
    <IsPackable>false</IsPackable>
    <!-- Disable SDK default glob so we control exactly what compiles. -->
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
  </PropertyGroup>
  <!-- Include all generated .g.cs files from the WakeLock profile output -->
  <ItemGroup>
    <Compile Include="**/*.g.cs" />
  </ItemGroup>
</Project>
